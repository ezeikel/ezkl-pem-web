import React from 'react';
import './Header.css';

const Header = (props) => {
  return (
    <header className="header row">
      <div className="logo"></div>
    </header>
  )
}

export default Header;
